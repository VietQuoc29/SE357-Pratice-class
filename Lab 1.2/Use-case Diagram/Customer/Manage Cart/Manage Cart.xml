@startuml
left to right direction
skinparam packageStyle rectangle

actor Customer as C
rectangle "Customer Manage Cart" {
  usecase (Manage Cart) as UC1
  usecase (Add Item to Cart) as UC2
  usecase (Remove Item from Cart) as UC3
  usecase (Update Cart) as UC4
}

C -- UC1

UC2 .> UC1 : extend
UC3 .> UC1 : extend
UC4 .> UC1 : extend
@enduml
